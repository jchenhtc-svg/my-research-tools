opencv-python-headless>=4.8.0
mediapipe==0.10.9
numpy>=1.24.0,<2.0.0
protobuf<4,>=3.11

# 網頁版後端（backend/app.py）需要，CLI版本不需要
flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1
gunicorn>=21.2.0   # Docker/雲端部署用；Windows不支援gunicorn，桌面版改用waitress

# 現地Windows桌面版（desktop_app.py / build_exe.bat）需要
waitress>=2.1.0
